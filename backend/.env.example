MONGO_URI=mongodb+srv://TuMongoDBConnectionString
PORT=8080
JWT_SECRET=carcaresaas-jwt-secret-key-change-me-in-prod
CORS_ORIGINS=https://saa-s-car-care-85l6.vercel.app,http://localhost:3000
RESEND_API_KEY=
RESEND_FROM_EMAIL=CarCare <onboarding@resend.dev>
# Sólo necesario mientras estés en modo sandbox de Resend (sin dominio verificado).
# Si lo seteás, todos los emails se redirigen a esta dirección con un banner que
# indica el destino original. Tiene que ser el mismo email con el que registraste
# tu cuenta en Resend, o Resend lo va a rechazar igual.
# Cuando verifiques tu dominio en https://resend.com/domains, borrá esta variable.
RESEND_SANDBOX_FALLBACK_EMAIL=